<h1>Burgers!</h1>

<h2>It's time to eat!</h2>

<ul>
  {{#each burgers}}
    {{#unless uneaten}}
      {{> burgers/burger-block uneaten=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>You ate these!</h2>

<ul>
  {{#each burgers}}
    {{#if uneaten}}
      {{> burgers/burger-block uneaten=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger Name:</label>
    <!--left off here-->
    <input type="text" id="ca" name="name">
  </div>

  <div class="form-group">
    <label for="uneaten">Eaten?</label><br>
    <input type="radio" name="uneaten" value="1" checked> Uneaten!<br>
    <input type="radio" name="uneaten" value="0"> Eaten
  </div>

  <button type="submit">Add Burger!!</button>
</form>
